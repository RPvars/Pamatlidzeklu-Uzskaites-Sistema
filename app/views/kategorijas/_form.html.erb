<%= form_with(model: kategorija) do |form| %>
  <% if kategorija.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(kategorija.errors.count, "error") %> prohibited this kategorija from being saved:</h2>

      <ul>
        <% kategorija.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nosaukums, style: "display: block" %>
    <%= form.text_field :nosaukums %>
  </div>

  <div>
    <%= form.label :apraksts, style: "display: block" %>
    <%= form.text_field :apraksts %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
