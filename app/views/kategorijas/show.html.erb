<p style="color: green"><%= notice %></p>
<%= render @kategorija %>
<div>
  <%= link_to "Rediģēt", edit_kategorija_path(@kategorija), class:"btn btn-secondary" %>
  <%= link_to "Visas kategorijas", kategorijas_path, class:"btn btn-primary" %>
  <div class="d-flex justify-content-end">
    <%= button_to "Dzēst", @kategorija, method: :delete, class:"btn btn-danger mt-3" %>
  </div>
</div>
