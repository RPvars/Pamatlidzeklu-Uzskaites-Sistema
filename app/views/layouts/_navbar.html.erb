<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container-fluid">
    <%= link_to "PaUS", root_path, class:"navbar-brand" %>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
      <ul class="navbar-nav me-auto">
        <% if user_signed_in?%>
          <li class="nav-item">
            <%= link_to "Home", root_path, class:"nav-link active" %>
          </li>
          <li class="nav-item">
            <%= link_to "Pamatlīdzekļi", pamatlidzeklis_path, class:"nav-link" %>
          </li>
          <% if current_user.admin? %>
            <li class="nav-item">
              <%= link_to "Kategorijas", kategorijas_path, class:"nav-link" %>
            </li>
          <% end %>
        <% end %>
      </ul>
      <ul class="navbar-nav">
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            <% if user_signed_in? %>
              Lietotājs
            <% else %>
              Autorizācija
            <% end %>
          </a>
          <ul class="dropdown-menu dropdown-menu-end">
            <% if user_signed_in? %>
              <li><%= button_to "Iziet", destroy_user_session_path, method: :delete, class:"dropdown-item" %></li>
              <li><%= link_to "Rediģēt Profilu", edit_user_registration_path, class:"dropdown-item" %></li>
            <% else %>
              <li><%= link_to "Izveidot lietotāju", new_user_registration_path, class:"dropdown-item" %></li>
              <li><%= link_to "Pieslēgties", new_user_session_path, class:"dropdown-item" %></li>
            <% end %>
          </ul>
        </li>
      </ul>
    </div>
  </div>
</nav>
